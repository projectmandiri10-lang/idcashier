# ================================
# SUPABASE CONFIGURATION
# ================================

# Supabase Project URL
# Get this from: Supabase Dashboard > Settings > API > Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous (Public) Key
# Get this from: Supabase Dashboard > Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (ADMIN ACCESS - Keep Secret!)
# Get this from: Supabase Dashboard > Settings > API > Project API keys > service_role
# Required for: Edge Functions, Admin operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here


# ================================
# SITE CONFIGURATION
# ================================

# Production Site URL
VITE_SITE_URL=https://idcashier.my.id
SITE_URL=https://idcashier.my.id


# ================================
# EMAIL / SMTP CONFIGURATION
# ================================
# Required for: Password reset, Email notifications

# SMTP Server Host
EMAIL_HOST=mail.idcashier.my.id

# SMTP Port (465 for SSL/TLS, 587 for STARTTLS)
EMAIL_PORT=465

# SMTP Username
EMAIL_USER=support@idcashier.my.id

# SMTP Password (Set in Supabase Dashboard > Settings > Secrets)
EMAIL_PASSWORD=your_smtp_password_here


# ================================
# SECURITY & AUTHENTICATION
# ================================

# JWT Secret for token signing
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_a_secure_random_32_byte_hex_string_here


# ================================
# CRONJOB CONFIGURATION
# ================================
# Required for: Demo account auto-reset

# Cronjob Secret Key for API authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRONJOB_SECRET=generate_another_secure_random_32_byte_hex_string_here

# Demo Account Email (for auto-reset)
DEMO_EMAIL=demo@gmail.com


# ================================
# CRONJOB URL (for hosting cron setup)
# ================================
# Format: https://YOUR_PROJECT_ID.supabase.co/functions/v1/demo-reset?secret=YOUR_CRONJOB_SECRET
# Example cronjob command for hosting:
# curl "https://eypfeiqtvfxxiimhtycc.supabase.co/functions/v1/demo-reset?secret=YOUR_CRONJOB_SECRET"


# ================================
# NOTES
# ================================
# 1. For Production: Set all these variables in Supabase Dashboard > Settings > Secrets
# 2. Never commit .env file with actual credentials to Git
# 3. Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. SUPABASE_SERVICE_ROLE_KEY has ADMIN access - keep it secret!
# 5. EMAIL_PASSWORD must match your SMTP server password
# 6. CRONJOB_SECRET is used by demo-reset Edge Function
# 7. Set up cronjob on your hosting to hit the demo-reset URL every hour

# ================================
# HOW TO USE
# ================================
# 1. Copy this file to .env: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# 2. Fill in all the values with your actual credentials
# 3. Run: npm install
# 4. Run: npm run build
# 5. Upload dist folder to your hosting

